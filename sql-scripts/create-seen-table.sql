DROP TABLE IF EXISTS seen;

CREATE TABLE seen (
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    user_id INTEGER REFERENCES users(id) NOT NULL,
    seen_id INTEGER REFERENCES users(id) NOT NULL
)