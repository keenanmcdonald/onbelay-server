DROP TABLE IF EXISTS rejects;

CREATE TABLE rejects (
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    user_id INTEGER REFERENCES users(id) NOT NULL,
    reject_id INTEGER REFERENCES users(id) NOT NULL
)